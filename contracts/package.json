{
  "name": "dao-proposal-scorer-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for DAO proposal scoring with Resonate",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.ts --network localhost",
    "node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@types/node": "^20.0.0",
    "hardhat": "^2.19.0",
    "typescript": "^5.3.0"
  }
}
